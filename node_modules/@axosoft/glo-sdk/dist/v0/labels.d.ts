import { AxiosInstance } from "axios";
export default class LabelFunctions {
    axios: AxiosInstance;
    constructor(axios: AxiosInstance);
    createLabel(boardId: string, name: string, color: Color): Promise<[Label]>;
    deleteLabel(boardId: string, labelId: string): Promise<{
        labelsRemoved: [string];
    }>;
    updateLabel(boardId: string, labelId: string, name?: string, color?: Color): Promise<[Label]>;
}
export declare type Label = {
    color: Color;
    created_by: string;
    created_date: string;
    id: string;
    name: string;
};
export declare type Color = {
    r: number;
    g: number;
    b: number;
};
